namespace Aula17Var
{
    class Program
    {
        static void Main(string[] args)
        {
            IEnumerable<MeuTipoComplexo> aluno = new IEnumerable<MeuTipoComplexo>();
            var aluno = new IEnumerable<MeuTipoComplexo>();

            //  NO COMEÇO DO CURSO QUERO FOCAR EM ESCREVER INT, FLOAT, STRING, CHAR.
            var idade = 25; // NÃO TEM DIFERENÇA NENHUMA OS DOIS ESTÃO CORRETO
            int idade = 25; // NÃO TEM DIFERENÇA NENHUMA OS DOIS ESTÃO CORRETO
            var nome = "andre";

        }
    }
}